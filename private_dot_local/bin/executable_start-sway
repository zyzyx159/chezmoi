#!/bin/sh

set -a

[ -f "$HOME/.config/sway/env" ] && . "$HOME/.config/sway/env"
[ -f "$HOME/.profile" ] && . "$HOME/.profile"

set +a

exec dbus-run-session sway --unsupported-gpu > /tmp/sway.log 2>&1
